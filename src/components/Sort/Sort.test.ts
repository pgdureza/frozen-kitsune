describe('Sort component', () => {
  describe('sorting', () => {
    it.todo('displays the sorting options')
    it.todo('changes selected sorting option when select is change')
  })
  describe('sort order', () => {
    it.todo('displays a order by button')
    it.todo('changes order by when button is clicked')
  })
})
